# Include once inside nginx http {} before the SOLE server blocks.
# These zones intentionally use only bounded network-level keys; Laravel retains endpoint-specific throttles.
limit_req_zone $binary_remote_addr zone=sole_api_general:10m rate=60r/s;
limit_conn_zone $binary_remote_addr zone=sole_connections:10m;
