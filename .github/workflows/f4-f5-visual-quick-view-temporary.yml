name: Temporary F4-F5 Visual Quick View

on:
  push:
    branches:
      - "phase/sole-f4-f5-catalog-product-card"

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase/sole-f4-f5-catalog-product-card
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.14"
          no-cache: true
      - run: bun install --frozen-lockfile
      - name: Apply stable Quick View capture
        run: |
          python3 scripts/patch-f4-f5-visual-quick-view.py
          bunx prettier --write scripts/visual-qa-f4-f5-catalog-product-card.mjs
          rm scripts/patch-f4-f5-visual-quick-view.py
      - name: Commit patch
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/visual-qa-f4-f5-catalog-product-card.mjs
          git add -u scripts/patch-f4-f5-visual-quick-view.py
          git commit -m "test(visual): activate visible Quick View trigger"
          git push origin HEAD:phase/sole-f4-f5-catalog-product-card
